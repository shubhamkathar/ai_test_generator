# ai_test_generator
Built an AI-powered automated testing framework that analyzes Python code using AST parsing, generates coverage-based test cases using Gemini API, converts them into executable pytest suites, runs them automatically, and includes an AI-driven feedback loop to improve failing tests.
